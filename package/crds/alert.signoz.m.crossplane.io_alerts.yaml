---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: alerts.alert.signoz.m.crossplane.io
spec:
  group: alert.signoz.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - signoz
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the Alerts API. Creates and manages alert
          resources in SigNoz.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AlertSpec defines the desired state of Alert
            properties:
              forProvider:
                properties:
                  alert:
                    description: |-
                      (String) Name of the alert.
                      Name of the alert.
                    type: string
                  alertType:
                    description: |-
                      (String) Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                      Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                    type: string
                  broadcastToAll:
                    description: |-
                      (Boolean) Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels. Deprecated: This field is no longer needed and will be ignored.
                      Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels.
                    type: boolean
                  condition:
                    description: |-
                      (String) Condition of the alert.
                      Condition of the alert.
                    type: string
                  description:
                    description: |-
                      (String) Description of the alert.
                      Description of the alert.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the alert is disabled.
                      Whether the alert is disabled.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) The evaluation window of the alert. By default, it is 5m0s.
                      The evaluation window of the alert. By default, it is 5m0s.
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation configuration of the alert as JSON. Only used when schema_version is v2 or higher.
                      Evaluation settings for the alert (JSON). Only used when schema_version is v2 or higher.
                    type: string
                  frequency:
                    description: |-
                      (String) The frequency of the alert. By default, it is 1m0s.
                      The frequency of the alert. By default, it is 1m0s.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels of the alert. Severity is a required label.
                      Labels of the alert. Severity is a required label.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: (Attributes) Notification settings for the alert.
                      Only used when schema_version is v2 or higher. (see below for
                      nested schema)
                    properties:
                      groupBy:
                        description: |-
                          (List of String) List of labels to group notifications by, use 'all' to get different notification for each unique parameters.
                          Group by the alert. By default, it is empty, use '__all__' to get different notification for each unique parameters.
                        items:
                          type: string
                        type: array
                      renotify:
                        description: (Attributes) Renotify settings for the alert.
                          (see below for nested schema)
                        properties:
                          alertStates:
                            description: |-
                              (List of String) List of alert states to trigger renotification. Possible values are: firing, nodata.
                              For which AlertStates of the alert want to get renotified.
                            items:
                              type: string
                            type: array
                          enabled:
                            description: |-
                              (Boolean) Whether renotify is enabled.
                              To enable the re-notification of the alert.
                            type: boolean
                          interval:
                            description: |-
                              (String) Renotification interval (e.g., "25m0s"). By default, it is empty.
                              Re-notify interval of the alert.
                            type: string
                        type: object
                      usePolicy:
                        description: |-
                          (Boolean) Whether to use notification policy. By default, it is false.
                          Use policy in the alert. By default, it is false.
                        type: boolean
                    type: object
                  preferredChannels:
                    description: |-
                      (List of String) Preferred channels of the alert. By default, it is empty.
                      Preferred channels of the alert. By default, it is empty.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Type of the alert. Possible values are: threshold_rule and promql_rule.
                      Type of the alert. Possible values are: threshold_rule and promql_rule.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                      Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                    type: string
                  severity:
                    description: |-
                      (String) Severity of the alert. Possible values are: info, warning, error, and critical.
                      Severity of the alert. Possible values are: info, warning, error, and critical.
                    type: string
                  source:
                    description: |-
                      (String) Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                      Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                    type: string
                  summary:
                    description: |-
                      (String) Summary of the alert.
                      Summary of the alert.
                    type: string
                  version:
                    description: |-
                      (String) Version of the alert. By default, it is v4.
                      Version of the alert. By default, it is v4.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alert:
                    description: |-
                      (String) Name of the alert.
                      Name of the alert.
                    type: string
                  alertType:
                    description: |-
                      (String) Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                      Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                    type: string
                  broadcastToAll:
                    description: |-
                      (Boolean) Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels. Deprecated: This field is no longer needed and will be ignored.
                      Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels.
                    type: boolean
                  condition:
                    description: |-
                      (String) Condition of the alert.
                      Condition of the alert.
                    type: string
                  description:
                    description: |-
                      (String) Description of the alert.
                      Description of the alert.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the alert is disabled.
                      Whether the alert is disabled.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) The evaluation window of the alert. By default, it is 5m0s.
                      The evaluation window of the alert. By default, it is 5m0s.
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation configuration of the alert as JSON. Only used when schema_version is v2 or higher.
                      Evaluation settings for the alert (JSON). Only used when schema_version is v2 or higher.
                    type: string
                  frequency:
                    description: |-
                      (String) The frequency of the alert. By default, it is 1m0s.
                      The frequency of the alert. By default, it is 1m0s.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels of the alert. Severity is a required label.
                      Labels of the alert. Severity is a required label.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: (Attributes) Notification settings for the alert.
                      Only used when schema_version is v2 or higher. (see below for
                      nested schema)
                    properties:
                      groupBy:
                        description: |-
                          (List of String) List of labels to group notifications by, use 'all' to get different notification for each unique parameters.
                          Group by the alert. By default, it is empty, use '__all__' to get different notification for each unique parameters.
                        items:
                          type: string
                        type: array
                      renotify:
                        description: (Attributes) Renotify settings for the alert.
                          (see below for nested schema)
                        properties:
                          alertStates:
                            description: |-
                              (List of String) List of alert states to trigger renotification. Possible values are: firing, nodata.
                              For which AlertStates of the alert want to get renotified.
                            items:
                              type: string
                            type: array
                          enabled:
                            description: |-
                              (Boolean) Whether renotify is enabled.
                              To enable the re-notification of the alert.
                            type: boolean
                          interval:
                            description: |-
                              (String) Renotification interval (e.g., "25m0s"). By default, it is empty.
                              Re-notify interval of the alert.
                            type: string
                        type: object
                      usePolicy:
                        description: |-
                          (Boolean) Whether to use notification policy. By default, it is false.
                          Use policy in the alert. By default, it is false.
                        type: boolean
                    type: object
                  preferredChannels:
                    description: |-
                      (List of String) Preferred channels of the alert. By default, it is empty.
                      Preferred channels of the alert. By default, it is empty.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Type of the alert. Possible values are: threshold_rule and promql_rule.
                      Type of the alert. Possible values are: threshold_rule and promql_rule.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                      Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                    type: string
                  severity:
                    description: |-
                      (String) Severity of the alert. Possible values are: info, warning, error, and critical.
                      Severity of the alert. Possible values are: info, warning, error, and critical.
                    type: string
                  source:
                    description: |-
                      (String) Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                      Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                    type: string
                  summary:
                    description: |-
                      (String) Summary of the alert.
                      Summary of the alert.
                    type: string
                  version:
                    description: |-
                      (String) Version of the alert. By default, it is v4.
                      Version of the alert. By default, it is v4.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.alert is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alert)
                || (has(self.initProvider) && has(self.initProvider.alert))'
            - message: spec.forProvider.alertType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.alertType)
                || (has(self.initProvider) && has(self.initProvider.alertType))'
            - message: spec.forProvider.condition is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.condition)
                || (has(self.initProvider) && has(self.initProvider.condition))'
            - message: spec.forProvider.severity is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.severity)
                || (has(self.initProvider) && has(self.initProvider.severity))'
          status:
            description: AlertStatus defines the observed state of Alert.
            properties:
              atProvider:
                properties:
                  alert:
                    description: |-
                      (String) Name of the alert.
                      Name of the alert.
                    type: string
                  alertType:
                    description: |-
                      (String) Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                      Type of the alert. Possible values are: METRIC_BASED_ALERT, LOGS_BASED_ALERT, TRACES_BASED_ALERT, and EXCEPTIONS_BASED_ALERT.
                    type: string
                  broadcastToAll:
                    description: |-
                      (Boolean) Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels. Deprecated: This field is no longer needed and will be ignored.
                      Whether to broadcast the alert to all the alerting channels. By default, the alert is only sent to the preferred channels.
                    type: boolean
                  condition:
                    description: |-
                      (String) Condition of the alert.
                      Condition of the alert.
                    type: string
                  createAt:
                    description: |-
                      (String) Creation time of the alert.
                      Creation time of the alert.
                    type: string
                  createBy:
                    description: |-
                      (String) Creator of the alert.
                      Creator of the alert.
                    type: string
                  description:
                    description: |-
                      (String) Description of the alert.
                      Description of the alert.
                    type: string
                  disabled:
                    description: |-
                      (Boolean) Whether the alert is disabled.
                      Whether the alert is disabled.
                    type: boolean
                  evalWindow:
                    description: |-
                      (String) The evaluation window of the alert. By default, it is 5m0s.
                      The evaluation window of the alert. By default, it is 5m0s.
                    type: string
                  evaluation:
                    description: |-
                      (String) Evaluation configuration of the alert as JSON. Only used when schema_version is v2 or higher.
                      Evaluation settings for the alert (JSON). Only used when schema_version is v2 or higher.
                    type: string
                  frequency:
                    description: |-
                      (String) The frequency of the alert. By default, it is 1m0s.
                      The frequency of the alert. By default, it is 1m0s.
                    type: string
                  id:
                    description: (String) Autogenerated unique ID for the alert.
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      (Map of String) Labels of the alert. Severity is a required label.
                      Labels of the alert. Severity is a required label.
                    type: object
                    x-kubernetes-map-type: granular
                  notificationSettings:
                    description: (Attributes) Notification settings for the alert.
                      Only used when schema_version is v2 or higher. (see below for
                      nested schema)
                    properties:
                      groupBy:
                        description: |-
                          (List of String) List of labels to group notifications by, use 'all' to get different notification for each unique parameters.
                          Group by the alert. By default, it is empty, use '__all__' to get different notification for each unique parameters.
                        items:
                          type: string
                        type: array
                      renotify:
                        description: (Attributes) Renotify settings for the alert.
                          (see below for nested schema)
                        properties:
                          alertStates:
                            description: |-
                              (List of String) List of alert states to trigger renotification. Possible values are: firing, nodata.
                              For which AlertStates of the alert want to get renotified.
                            items:
                              type: string
                            type: array
                          enabled:
                            description: |-
                              (Boolean) Whether renotify is enabled.
                              To enable the re-notification of the alert.
                            type: boolean
                          interval:
                            description: |-
                              (String) Renotification interval (e.g., "25m0s"). By default, it is empty.
                              Re-notify interval of the alert.
                            type: string
                        type: object
                      usePolicy:
                        description: |-
                          (Boolean) Whether to use notification policy. By default, it is false.
                          Use policy in the alert. By default, it is false.
                        type: boolean
                    type: object
                  preferredChannels:
                    description: |-
                      (List of String) Preferred channels of the alert. By default, it is empty.
                      Preferred channels of the alert. By default, it is empty.
                    items:
                      type: string
                    type: array
                  ruleType:
                    description: |-
                      (String) Type of the alert. Possible values are: threshold_rule and promql_rule.
                      Type of the alert. Possible values are: threshold_rule and promql_rule.
                    type: string
                  schemaVersion:
                    description: |-
                      (String) Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                      Schema version of the alert. By default, it is v1. For v2+ schemas, additional fields like evaluation and notification_settings are supported.
                    type: string
                  severity:
                    description: |-
                      (String) Severity of the alert. Possible values are: info, warning, error, and critical.
                      Severity of the alert. Possible values are: info, warning, error, and critical.
                    type: string
                  source:
                    description: |-
                      (String) Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                      Source of the alert. By default, it is <SIGNOZ_ENDPOINT>/alerts.
                    type: string
                  state:
                    description: |-
                      (String) State of the alert.
                      State of the alert.
                    type: string
                  summary:
                    description: |-
                      (String) Summary of the alert.
                      Summary of the alert.
                    type: string
                  updateAt:
                    description: |-
                      (String) Last update time of the alert.
                      Last update time of the alert.
                    type: string
                  updateBy:
                    description: |-
                      (String) Last updater of the alert.
                      Last updater of the alert.
                    type: string
                  version:
                    description: |-
                      (String) Version of the alert. By default, it is v4.
                      Version of the alert. By default, it is v4.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
